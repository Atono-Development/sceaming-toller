# Resend Email Service
# Sign up at https://resend.com and generate an API key
RESEND_API_KEY=re_your_api_key_here

# Application URL (for invitation links)
# Local development uses port 5173 (see docker-compose.yml)
# Production should be your actual domain (e.g., https://yourdomain.com)
APP_URL=http://localhost:5173

# Email Configuration
# This should be a verified domain in your Resend account
FROM_EMAIL=noreply@yourdomain.com

# Database Configuration (if not using docker-compose defaults)
# DB_URL=postgres://user:password@localhost:5432/screaming_toller?sslmode=disable

# JWT Secret (REQUIRED in production)
# JWT_SECRET=your_secure_random_secret_here
